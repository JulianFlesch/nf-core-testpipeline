ci:
  # Preserves this pipeline's existing nf-test tuning; nf-core/actions'
  # own built-in defaults are nf_test_version 0.9.5 and max_shards 20.
  nf_test_version: "0.9.4"
  max_shards: 7
lint: {}
nf_core_version: 4.1.0
repository_type: pipeline
template:
  author: nf-core
  description: An example pipeline used to test new nf-core infrastructure and common
    code.
  force: false
  is_nfcore: true
  name: testpipeline
  org: nf-core
  outdir: .
  version: 1.0dev
